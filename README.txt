


how to checkout old branches:


git checkout master              # switch to the master branch
git reset --hard <branch name>     # point HEAD to the clean_start branch
git push -f origin master  # force push the new HEAD to server
